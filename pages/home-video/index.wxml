<!--pages/home-video/index.wxml-->
<view class="video">
    <block wx:for="{{topMvs}}" wx:key="id">
        <view class="item">
            <video-item-vedio1 item="{{item}}"
            bind:tap="handleVideoItemClick"
            data-item="{{item}}"
            ></video-item-vedio1>
        </view>
    </block>
</view>